# 开发环境配置
# 作者: mumu
# 描述: 开发环境专用配置，CORS使用通配符便于开发调试

# CORS跨域配置（开发环境）
woodlin:
  cors:
    # 启用CORS
    enabled: true
    # 允许所有源（开发环境使用通配符）
    allowed-origins:
      - "*"
    # 允许的HTTP方法
    allowed-methods:
      - GET
      - POST
      - PUT
      - DELETE
      - OPTIONS
      - PATCH
    # 允许的请求头
    allowed-headers:
      - "*"
    # 暴露的响应头
    exposed-headers:
      - Authorization
      - Content-Type
      - X-Total-Count
    # 使用通配符时，allowCredentials必须为false
    allow-credentials: false
    # 预检请求缓存时间（秒）
    max-age: 3600
    # 应用的路径模式
    path-pattern: "/**"
